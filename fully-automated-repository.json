{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Renovate config for fully automated repositories (automatically tested e.g. with preview deployments) - extends the default config.",
  "extends": ["github>promise/renovate-config", "github>promise/renovate-config:automerge"]
}